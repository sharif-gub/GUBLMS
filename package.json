{
  "name": "library-management-system",
  "version": "1.0.0",
  "description": "A full-featured Library Management System with borrow, return, and fine calculation",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev",
    "start": "node api/index.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "pg": "^8.11.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
